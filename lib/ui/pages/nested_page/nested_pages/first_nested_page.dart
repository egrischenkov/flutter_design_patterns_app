import 'package:flutter/material.dart';

class FirstNestedPage extends StatelessWidget {
  const FirstNestedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('First'),
    );
  }
}
