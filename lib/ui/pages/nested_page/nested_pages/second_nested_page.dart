import 'package:flutter/material.dart';

class SecondNestedPage extends StatelessWidget {
  const SecondNestedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Second'),
    );
  }
}
