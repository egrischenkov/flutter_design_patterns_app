import 'package:flutter/material.dart';

class AbstractFactoryPatternWidget extends StatelessWidget {
  const AbstractFactoryPatternWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
